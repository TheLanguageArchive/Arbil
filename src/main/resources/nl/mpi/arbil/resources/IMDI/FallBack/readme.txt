The files in this directory are retrieved from the web server at compile time.
They will only used if the server is not available on the first run and will
be replaced (and regularly updated) by the application when a network
connection is available.